@{
    var active = ViewData["Active"] != null ? ViewData["Active"].ToString() : "Admin";
}
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container" id="">
        <ul class="navbar-nav mr-auto">
            <li>
                <a class="nav-link @(active == "Admin" ? "active" : "")" asp-controller="Admin" asp-action="Index">Home</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-toggle="dropdown" aria-expanded="false">
                    Item
                </a>
                <div class="dropdown-menu">
                    <a class="dropdown-item" asp-controller="Item" asp-action="Index">View Item List</a>
                    <a class="dropdown-item" asp-controller="Item" asp-action="Create">Enroll New Item Code</a>
                </div>
            </li>
        </ul>
        <form class="form-inline" method="post" asp-controller="Admin" asp-action="Logout">
            <button class="btn btn-secondary btn-sm">Logout</button>
        </form>
    </div>
</nav>